import { useEffect, useState } from 'react';
import { Database, ExternalLink, Search, ShieldCheck } from 'lucide-react';
import { api } from '../api.js';
export function SourcesView(){const[data,setData]=useState(null);const[q,setQ]=useState('');useEffect(()=>{api.sources().then(setData)},[]);const items=(data?.items||[]).filter(i=>i.name.toLowerCase().includes(q.toLowerCase()));return <div className="page-content"><section className="page-header"><div><span className="eyebrow">Rastreabilidade</span><h1>Fontes e evidências</h1><p>Veja quais bases sustentam os indicadores e quantas descobertas ainda dependem de revisão.</p></div></section><div className="filter-bar"><label className="search-field"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Pesquisar IBGE, SINISA, SAAE..."/></label></div><div className="source-grid">{items.map(s=><article className="source-card" key={s.name}><div className="source-icon"><Database size={20}/></div><div className="source-title"><strong>{s.name}</strong><span><ShieldCheck size={13}/> {s.uses} uso(s) na base · {s.findings} descoberta(s)</span></div><div className="source-count"><b>{s.indicatorCount}</b><small>indicadores relacionados</small></div>{s.urls[0]?<a href={s.urls[0]} target="_blank" rel="noreferrer">Abrir fonte <ExternalLink size={14}/></a>:<span className="muted-link">URL ainda não registrada</span>}</article>)}</div></div>}
